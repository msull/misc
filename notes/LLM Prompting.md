

```

{
  "<url>": {
    "<METHOD>": {
      "description": "<description_text>",
      "exampleCall": "<METHOD> <url>?<query_params>",
      "requestBody": "<request_body_object>",
      "exampleResponse": "<response_object>"
    }
    // Additional methods (GET, POST, etc.) for the same URL can be added here
  }
  // Additional URLs follow the same structure
}
```
